CREWBIDIQ v0.3.1b — ONE-TIME CLEAN REPOSITORY REPLACEMENT

This archive is already structured as the GitHub repository root.

1. Unzip this package.
2. Open your CrewBidIQ repository root in Working Copy or on a computer.
3. Remove the old application files/folders from the repository root, but keep the hidden .git folder.
4. Copy EVERYTHING INSIDE this extracted folder into the repository root.
5. Confirm the repository root directly contains:
   app/
   tests/
   data/
   Dockerfile
   render.yaml
   requirements.txt
   README.md
6. Commit with: v0.3.1b complete upload fix
7. Push to main. Render should auto-deploy.

Do not place this folder itself inside the repository. Copy its contents.
